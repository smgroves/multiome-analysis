# Domain-shift diagnostic: organoid/mets_compiled vs. in-vivo GEMM

*(All scripts named below live under `network-inference-DIRECT-NET/claude_analysis/`,
organized by phase: `01_normalization_sweep/`, `02_hyperparameter_experiments/`,
`03_domain_shift_diagnostics/`, `04_rorb_perturbation_validation/`, `05_preprocessing/`.)*

Generated by `diagnose_domain_shift_6667.py`. Question: is the shGFP organoid (and
mets_compiled) external-validation shortfall explained by cell-culture/metastatic-context
stress rather than by boba-T's fitted rules failing to generalize?

## 1. Continuous stress score: weak, inconsistent signal

Per-cell `|predicted - actual|` residual for each dataset's 10 worst-validating genes,
correlated against that dataset's own precomputed continuous stress score
(`stress_score1` for organoid, `Stress_score1` for mets_compiled): 27/40 gene×dataset
pairs are nominally significant (p<0.05, large N makes this easy), but effect sizes are
tiny (|r| mostly <0.2) and **the sign is inconsistent** — about half positive, half
negative. A genuinely stress-driven confound should show a stronger, consistently
positive correlation (higher stress -> higher error) across most genes. It doesn't. The
continuous stress score, by itself, does not explain most of the external-validation
error.

## 2. Discrete subpopulation identity: a real, more specific signal

Grouping the same residuals by discrete cell-type call (organoid's `predicted.id`) or
cluster (mets_compiled's `seurat_clusters`) tells a sharper story:

- **mets_compiled, cluster `2`** (1221 cells — the 3rd largest cluster, not a small/noisy
  group) is the single highest-residual cluster for **7 of the 10** worst-validating
  genes (`FOXO3`, `RORA_RORB`, `ETS1`, `TBX15`, `KMT2A`, `ICAM1`, `SMAD3`). This is a
  concentrated, well-powered signal: whatever's driving mets_compiled's validation
  shortfall is disproportionately located in this one subpopulation, not spread evenly
  across the dataset.
- **organoid, cell-type `"Stress"`** is the highest-residual group for 4/10 genes in
  shGFP and 5/10 in shRORB2 — also enriched, but this population is small (90 cells in
  the full organoid dataset, fewer still per condition split), so this read is more
  noise-prone than mets_compiled's cluster 2 and should be treated as suggestive rather
  than conclusive on its own.

## Interpretation

This argues against a simple "the whole null condition is uniformly worse because
cultured/metastatic cells are stressed" story. Instead, it points to a **specific
subpopulation** — most clearly mets_compiled's cluster 2 — as the actual locus of
mismatch. Two consistent explanations: (a) that subpopulation is genuinely
out-of-distribution relative to the GEMM allograft data the rules were fit on (a real
domain-shift pocket, not a uniform effect), or (b) it represents a real biological state
that the 53-node/228-edge network's regulator set doesn't capture well, independent of
culture stress per se. Either way, the fix isn't "the whole shGFP/mets_compiled dataset
is untrustworthy as a validation set" — most of each dataset validates fine; a bounded
subpopulation is where the fit degrades. Worth a closer look at what distinguishes
mets_compiled's cluster 2 biologically (marker genes, `Int_score1`/`NE1_score1`/etc.
archetype scores) before drawing further conclusions.

## Caveats

- The organoid "Stress" cell-type finding is based on a small population (~90 cells
  total in the unsplit organoid dataset) and should be corroborated with a larger stress
  population before treating it as equally solid as the mets_compiled cluster-2 finding.
- This diagnostic used each dataset's own pre-existing score/cluster columns, not a new
  independent stress signature — it inherits whatever those upstream calls already
  captured or missed.

## 3. What mets_compiled's cluster 2 actually is (`characterize_mets_cluster2.py`)

Followed up on §2's "closer look" with a full differential-expression pass (whole
preprocessed gene panel, not just network genes) plus an attractor-distance check.

**It's a dissociation/acute-stress artifact signature, not a distinct tumor archetype.**
Cluster 2's top differentially-*higher* genes are dominated by canonical immediate-early
genes induced by tissue dissociation during single-cell prep: `EGR1` (+0.218, the single
largest difference in either direction), `FOS` (+0.087), `JUN` (+0.069), `FOSB` (+0.039)
— a well-documented scRNA-seq technical confound (e.g. van den Brink et al. 2017). A
combined IEG score (`EGR1`/`FOS`/`JUN`/`FOSB`/`JUND`/`JUNB` mean) is markedly higher in
cluster 2 (0.225 vs. 0.152), consistent with the dataset's own `Stress_score1` also being
elevated there (+0.037, §2). Overlaid on this stress program, cluster 2 shows **suppressed
lineage-identity TFs**: `NFIB` (-0.262), `ZBTB20` (-0.206), `ZEB1` (-0.130), `TCF4`
(-0.123), `RORB` (-0.115), `SOX11` (-0.112), `RORA_RORB` (-0.064), `TBX15` (-0.033) — most
of the network's own worst-validating genes in this dataset (`RORA_RORB`, `TBX15`, `NCAM1`,
`KMT2A`) are among these suppressed-identity genes, while others (`ICAM1`, `SMAD3`,
`NFKB1`) have technically-significant but near-zero absolute differences (both means near
0), i.e. not a biologically meaningful shift for those three specifically.

**Not an "unseen attractor."** Checked whether cluster 2's mean continuous state (network
genes only) sits distinctly closer to, or further from, any of 6667's 8 characterized GEMM
attractors (`6667/attractors/average_states.txt`: `Generalist_NE`/`Generalist_nonNE`/
`Arc_1`-`Arc_6`) than the rest of mets_compiled does. It doesn't — cluster 2's and the
rest's distances to every attractor are within ~0.15 of each other, both closest to `Arc_3`
(Secretory) then `Generalist_NE`, in the same rank order. So this isn't a case of a genuinely novel
biological state that a correctly-generalizing Boolean network should still resolve to a
known attractor (the claim BoBa-T makes about unseen clusters) — cluster 2 isn't
"unseen" in that sense at all. It's the *same* underlying identity as the rest of the
dataset, with an acute technical stress-response program temporarily overwriting part of
the measured transcriptional readout. A model fit on identity-driven regulatory dynamics
has no reason to predict a dissociation artifact, and arguably shouldn't be expected to —
this is a data-quality caveat for mets_compiled specifically, not a BoBa-T generalization
failure.

Outputs: `mets_cluster2_differential_expression.csv` (full gene panel),
`mets_cluster2_attractor_distances.csv`.

## 4. organoid_shGFP: correlation-structure preservation predicts R², but organoid is not a categorical outlier (`diagnose_organoid_shgfp.py`, `compare_correlation_shift_across_samples.py`)

**(a) It's not a set of hard genes — it's the organoid context.** Every one of shGFP's
worst-validating genes validates *excellently* in-sample (6667's own held-out GEMM test
set): mean in-sample R² = **0.855**, vs. mean shGFP R² = **0.023**. Zero of 53 genes are
bad in both (`organoid_shgfp_insample_vs_external_r2.csv`). `LMX1B` alone goes from
R²=0.91 in-sample to R²=-0.84 in shGFP. These genes are not intrinsically difficult for
BoBa-T to predict — the fitted rules describe them very well when the underlying data
matches the training context. This part holds up.

**(b) Correlation-structure preservation genuinely predicts R² — but as a general,
continuous, cross-sample relationship, not an organoid-specific effect.** *(Correction:
an earlier version of this section framed the organoid finding as "in-vitro vs in-vivo."
That framing doesn't survive checking against the full sample set below and has been
replaced.)* Loaded GEMM training data and organoid_shGFP through the identical
`node_normalization=0.3` pipeline and compared the Pearson correlation of every one of the
network's 226 fitted gene-regulator edges between the two datasets: 42% of all edges flip
sign, 38% of the *strong* GEMM edges (|r|>0.5) still flip. Taken alone this looked like a
smoking gun for organoid specifically. But repeating the same edge-correlation comparison
for **all 33 of 6667's other external validation samples with a scored `summary_stats.csv`**
(12 allografts, 20 human tumors, plus mets_compiled) shows organoid_shGFP's numbers
(overall edge-correlation 0.27, strong-edge sign-flip rate 0.38) are unremarkable within
that full population — several allograft and human tumor samples show similarly or more
degraded correlation preservation. What *does* hold up as a strong, general predictor
across all 33 samples: **the fraction of GEMM's strong edges that flip sign correlates
with that sample's mean R² at r=-0.72** (r=-0.68 using all edges, not just strong ones;
r=0.15 for raw correlation-of-correlations, which is noisy and not a useful single
predictor). This is not primarily a cell-count artifact (`corr(n_cells,
strong_sign_flip_rate)` = 0.12-0.26 across two runs, much weaker than the -0.72 relationship
itself).

**(c) organoid_shGFP and mets_compiled sit at the bad tail of that same population-wide
relationship — alongside the worst human tumor and allograft samples, not in a separate
category.** Bottom 8 of all 33 scored samples by mean R²:

| sample | category | strong_sign_flip_rate | mean R² |
|---|---|---|---|
| mets_compiled | mets_compiled | 0.311 | 0.019 |
| organoid_shGFP | organoid | 0.377 | 0.023 |
| human_RU1215 | human tumor | 0.358 | 0.033 |
| human_RU1108 | human tumor | 0.318 | 0.078 |
| human_RU1322 | human tumor | 0.311 | 0.097 |
| human_RU1145 | human tumor | 0.351 | 0.098 |
| organoid_combined | organoid | 0.338 | 0.100 |
| allograft_mt3 | allograft | 0.311 | 0.110 |

`human_RU1215` — a different species, unambiguously in vivo — validates barely better than
organoid_shGFP, with a comparably high strong-edge sign-flip rate. Meanwhile the *best*
allograft (`TKO-luc`, R²=0.55) and best human tumor (`RU1311`, R²=0.54) both have low
sign-flip rates (0.16, 0.25). Full table: `all_samples_corr_vs_r2.csv`.

**Bottom line, corrected: this is not primarily a BoBa-T fitting problem, and — as far as
checked — not primarily an in-vitro-vs-in-vivo effect either.** Whatever makes a given
external sample's gene-regulator correlation structure diverge from GEMM's — which varies
continuously across allografts, human tumors, *and* organoid, not by species or culture
status — is the real predictor of how well that sample validates. organoid_shGFP happens
to be one of the more extreme cases of this, on par with the single worst human tumor
sample rather than qualitatively different from the in-vivo samples. What specifically
drives any one sample's correlation-structure divergence (tumor subtype/lineage-state
composition, technical depth, something else) is still open — see §5 for how to chase
this without assuming a category (in-vitro, species, etc.) in advance.

Outputs: `organoid_shgfp_insample_vs_external_r2.csv`,
`organoid_shgfp_edge_correlation_shift.csv`, `allograft_control_summary.csv`,
`all_samples_corr_vs_r2.csv`.

## 5. Distance from the training distribution (UMAP, kNN, attractor distance) is a much weaker predictor than correlation-structure preservation (`umap_archetype_distance_analysis.py`)

Direct, assumption-free test of §4's open question, without presupposing any category
(in-vitro, species, etc.): does a sample simply being "far away" from the GEMM training
distribution predict its R², rather than (or in addition to) the specific gene-gene
correlation-structure preservation metric in §4? Pooled GEMM training cells with every
scored sample's cells (raw, comparable log1p+MAGIC-imputed units, 1500 cells/sample cap),
fit one shared UMAP embedding, and computed three distance metrics per sample against the
same 33-sample population used in §4:

| metric | corr with mean R² |
|---|---|
| strong-edge sign-flip rate (§4, for reference) | **-0.72** |
| distance to nearest of 6667's 8 known attractors | -0.49 |
| mean kNN distance to GEMM training cells, raw 53-gene space (k=15) | -0.32 |
| UMAP-space centroid distance from GEMM training centroid | -0.09 (no relationship) |

*(Corrected after an initial bug: the first pass loaded `6667/data_split/train_t0combined.csv`
with `norm=None` intending "raw GEMM data," but that file is already
`node_normalization=0.3`-clipped — confirmed directly, exactly 60% of any gene's values are
exactly 0 or 1 — so GEMM's side of the comparison was in the wrong units against every
external sample's genuinely-raw values. Fixed by loading the true raw source file
(`data/adata_imputed_combined_v3_RORA_RORB_ave.csv`), restricted to the saved training-cell-ID
list. kNN distance moved from -0.22 to -0.32; UMAP centroid distance stayed near zero either
way, -0.09 now vs. 0.05 before — still not a useful metric here.)*

All three distance metrics point the *expected* direction (farther -> lower R², except
UMAP centroid distance which shows no relationship at all — consistent with UMAP's known
unreliability for preserving global/between-cluster distances, so this one is not a
useful metric here). But none of them come close to the correlation-structure metric's
-0.72. Concretely: nearest-attractor distance barely varies across the *entire* sample
population (3.44 to 3.65 across all 33 samples) — the worst-validating samples
(`mets_compiled` 3.63, `organoid_shGFP` 3.60, `human_RU1215` 3.65) are not meaningfully
farther from a known attractor than the best (`allograft_TKO-luc` 3.52, `human_RU1311`
3.44).

**Interpretation**: whether a sample's *average expression state* resembles GEMM's (or a
known attractor) is not what predicts validation quality — a sample can have an
unremarkable, GEMM-like mean profile and still validate badly, if the specific gene-gene
covariance structure BoBa-T's rules depend on has shifted. This sharpens §4's finding
rather than replacing it: the useful direction to chase what's *driving* a sample's
correlation-structure divergence is not "how different is this sample's cell population,
overall" but something more specific to pairwise regulatory relationships — e.g.
lineage/archetype *composition* (mixture of subpopulations, not just mean position),
technical depth, or something not yet checked, rather than a simple distance-from-training
signal in expression or embedding space.

Outputs: `umap_embedding_all_samples.csv` (per-cell, for visualization),
`umap_archetype_distance_vs_r2.csv`.

## 6. Why the correlation structure shifts: restriction of range (`diagnose_sample_diversity.py`)

**Fair challenge to §4-5: "strong-edge sign-flip rate predicts R²" is close to
tautological on its own.** BoBa-T's rules *are* that correlation structure, so of course a
sample whose gene-gene relationships have shifted gets predicted poorly — that's a
restatement of the mechanism, not an explanation of *why* the structure shifts for some
samples and not others. This section tests the most direct candidate mechanism.

**Hypothesis: restriction of range.** If a sample's cells span less of the underlying
cell-state diversity that GEMM's training tumors span (fewer distinct archetypes/less
spread along the identity axes the network's genes vary along), within-sample gene-gene
correlations will attenuate or flip sign as a *statistical byproduct* of reduced variance
— independent of any real biological rewiring — and, separately but for the same
underlying reason, prediction will be poor because the fitted rules were calibrated to
distinguish states across a range of variation that sample barely explores. This would
give a real, mechanistic "why," not just a restatement.

**Result: supported.** Computed each sample's mean per-gene raw standard deviation
(pre-normalization, comparable log1p+MAGIC-imputed units) relative to GEMM training data's
own: **this "relative diversity" measure correlates with mean R² at r=0.58** — notably
higher than the UMAP/attractor distance metrics in §5, and only weakly related to the
sign-flip-rate metric itself (r=-0.23 between the two), so it is capturing something
distinct, not just re-deriving §4's number by another name. *(This ratio was computed
against the correct raw GEMM reference from the start — the correlation is scale-invariant
to the §5 bug, since every sample was divided by the same GEMM constant either way.)*

| sample | relative diversity vs. GEMM | mean R² |
|---|---|---|
| mets_compiled | 0.29 (lowest of all 33) | 0.019 |
| human_RU1215 | 0.55 | 0.033 |
| allograft_mt3 | 0.44 | 0.110 |
| ... | ... | ... |
| allograft_TKO-luc | 1.21 | 0.550 |
| human_RU1311 | **1.71 (highest of all 33)** | 0.538 |

`mets_compiled` — the single worst-validating sample — is also the single least-diverse
sample by this measure; the two best-validating samples (`TKO-luc`, `RU1311`) are among
the most diverse (both, notably, *more* diverse than GEMM's own training data — ratio >1).
**Honest caveat: this doesn't fully explain organoid_shGFP specifically** — its relative
diversity (0.73) is middling-to-high, not the lowest, yet its R² (0.023) is nearly as bad
as mets_compiled's. So low diversity is sufficient to predict bad validation but not
necessary. §7 chases the "second contributing cause" directly.

**Practical implication**: this reframes what "the network doesn't generalize to organoid"
means. It's closer to "the network can't be expected to resolve identity in a sample that
doesn't explore much of the identity-axis variation it was fit to distinguish" — a
statement about the *validation sample*, not a defect in BoBa-T's fitted rules. For the
paper, this argues for reporting/interpreting external validation scores alongside a
diversity or archetype-composition measure of the validation set itself, rather than
treating "external R²" as a single comparable number across samples that differ widely in
how much relevant biological variation they contain.

Outputs: `sample_diversity_vs_signflip_r2.csv`.

## 7. Not just how much variance, but which axis: why "orthogonal" variance isn't actually harmless (`diagnose_variance_composition.py`)

**Fair challenge to §4/§6: sign-flip-rate <-> R² is close to tautological** (BoBa-T's rules
*are* the correlation structure, so a shift there predicting poor prediction restates the
mechanism, not the cause) **and raises a real theoretical question**: if a sample has a lot
of variance orthogonal to GEMM's known identity axes, why should that matter for how well
the network predicts genes *along* those identity axes? Two candidate answers, tested
directly rather than assumed:
(a) low-diversity samples simply lack enough of the *right* variance for R² to be
well-defined regardless of model quality (a restatement of §6's finding, not new), or
(b) "orthogonal" variance isn't actually harmless because several of the network's own
genes (`JUN`, `JUND`, `JUNB`, `FOS`, `EGR1`) are simultaneously (i) targets of whatever
drives that orthogonal axis (e.g. acute stress/dissociation, per §3) and (ii) *fitted
regulators BoBa-T's rules use for other genes* (`ASCL1`, `HES1`, `LMX1B`, `TCF7L1`, and
others all have one of these as a regulator). A rule has no way to know a regulator's
value moved for a reason unrelated to the identity logic it encodes — it just reads the
value as given. So stress-driven movement in these specific genes doesn't stay
"orthogonal" to the identity axes in any way that matters to the model; it directly
corrupts the input to every downstream prediction that uses one of them as a regulator.

**Method**: fit PCA on GEMM's raw training data (correct raw file, restricted to the saved
train-cell-ID list) to define GEMM's own top-5 "identity axes" (explaining 97% of GEMM's
own variance; PC1 alone is essentially an `ASCL1`/`MEIS2` axis, PC3 is essentially an
`EGR1`/`FOS`-vs-`SOX11` axis — i.e. GEMM's own top axes already partially separate an
IEG-like direction from lineage-TF directions). Projected every external sample onto this
*fixed* basis (GEMM's own mean + loadings — not a per-sample refit) and computed, for each
sample, what fraction of *its own* total variance lands on GEMM's identity axes vs. on the
specific IEG-gene subspace.

**Result**: `identity_pc_var_frac` correlates with mean R² at **r=0.65** — the best of
every distance/composition metric tried so far, and more mechanistically specific than
§6's raw diversity number (r=0.58) since it asks not just "how much variance" but "how
much of it is on the axes that matter." `ieg_var_frac` correlates *positively* with R²
(r=0.43) — more IEG variance associates with *better* validation, which on its own does
not support "IEG variance is categorically bad." Resolving this against organoid_shGFP
specifically clarifies why:

| sample | identity_pc_var_frac | ieg_var_frac | relative diversity | mean R² |
|---|---|---|---|---|
| organoid_shGFP | 0.21 | **0.005 (lowest of all 33)** | 0.73 | 0.023 |
| mets_compiled | 0.27 | 0.13 | 0.29 | 0.019 |

**organoid_shGFP's problem is not excess IEG/stress variance crowding out identity
signal** (the hypothesis suggested by §3's mets_compiled cluster-2 finding) — its IEG
variance fraction is the *lowest* of any of the 33 samples, essentially flat. Its identity-
axis fraction is also low (0.21), and since identity (0.21) + IEG (0.005) accounts for only
~22% of its variance while its overall diversity is moderate-to-high (0.73), **most of
organoid_shGFP's variance sits on neither GEMM's identity axes nor the IEG axis** — some
other, uncharacterized source (candidates: organoid-culture-specific technical/batch
variance, a genuine biological axis not present in GEMM, or noise from a different
imputation/depth regime). `mets_compiled`, by contrast, fits the original stress-crowding
story reasonably well: low total diversity (0.29, the lowest of all 33) *and* a
meaningful share of what diversity it has is on the IEG axis (0.13, second-highest of the
bottom-8 group) — consistent with §3's direct characterization of its cluster 2.

**So the two worst-validating samples fail for different specific reasons**, both
downstream of the same general principle (§4-§7): validation quality tracks how much of a
sample's variance sits on the axes BoBa-T's rules actually use, not how much variance it
has overall, and not how far its average state sits from training data (§5).

Outputs: `variance_composition_vs_r2.csv`.

## 8. organoid_shGFP's "uncharacterized" 78% is real biology, just not aligned to GEMM's specific axes (`diagnose_organoid_shgfp_unexplained_variance.py`)

§7 found little of organoid_shGFP's variance lands on GEMM's identity axes or the IEG
axis. Two very different explanations were live: (a) that ~78% is noise/technical
artifact, or (b) it's real biological heterogeneity that simply isn't oriented the same
way as GEMM's specific PCA axes. Checked directly by fitting PCA on organoid_shGFP's *own*
raw data (its own dominant directions of variation, not GEMM's basis) and correlating each
top PC's per-cell scores against every available archetype/lineage/technical metadata
column in `organoid_clusters.csv`.

**Answer: (b). This is real, substantial, biologically-coherent heterogeneity.**
organoid_shGFP's own PC1 (47% of its variance) correlates with `prediction.score.Intermediate`
(r=0.39), `HES1_high_score1` (r=0.33), and `Ascl1_targets_score1` (r=-0.31) — an
Intermediate-vs-ASCL1/NE identity axis. PC2 (19% of variance) correlates strongly with
`NE2_score1` (r=0.64), `Rest_targets_score1` (r=0.62), `Ascl1_targets_score1` (r=0.58),
and `Intermediate_score1` (r=-0.55) — a strong NE-lineage identity axis. Categorically,
fine-grained subcluster identity (`tree.ident`, 21 groups) explains 51% of PC1's variance
and 33% of PC2's (`seurat_clusters`/`rcpa_clusters`, 8 groups, explain 34% and 24%
respectively). Together PC1+PC2 account for ~65% of organoid_shGFP's total variance and
are both cleanly, substantially explained by real lineage/archetype identity — not
technical depth (`nFeature_RNA`/`gene_count` only show up weakly, on PC3, r=0.27) and not
noise.

**So organoid_shGFP is not flat or uninformative** — it has real cell-state heterogeneity
along recognizable NE/Intermediate/ASCL1 axes, the same kind of biology GEMM's identity
axes represent. The reason so little of it landed on *GEMM's specific* PCA axes in §7 is
not that the biology is absent, but that organoid's genes covary along a differently
-oriented combination than GEMM's — the same underlying phenomenon as §4's finding that
many specific gene-gene correlations shift or flip sign between organoid and GEMM. This
ties §4, §7, and §8 into one consistent picture: the relevant lineage biology is present in
organoid_shGFP, but the *specific linear combination of genes* that expresses it differs
from GEMM's, so a rule set built on GEMM's specific combination doesn't transfer even
though both systems are exploring recognizably the same biology.

Outputs: `organoid_shgfp_own_pc_metadata_correlation.csv`,
`organoid_shgfp_own_pc_categorical_variance.csv`.

## 9. Resolved: genuine rewiring, not a population-mixture artifact (`diagnose_leaf_conditional_agreement.py`)

§8 leaves one loose end: if organoid_shGFP varies along the *same genes* as GEMM's
identity axes, why would their specific relationships differ — shouldn't the same
regulatory logic apply to both? There are two very different explanations, and marginal
pairwise correlation (§4) cannot tell them apart:

- **Composition artifact**: BoBa-T's own model says a gene's relationship to one regulator
  can flip sign depending on a *second* regulator's state — that's what a multi-regulator
  truth table encodes (conditional, not simple pairwise, logic). If GEMM and organoid cells
  have a different *mixture* of full regulator-combination states, the marginal
  correlation between any single regulator pair can look totally different even if every
  individual fitted truth-table entry is unchanged. Under this explanation, the actual
  logic transfers fine; only the population-level summary (correlation, R²) looks bad.
- **Genuine rewiring**: the same regulator combination really does imply a different
  target value in organoid vs. GEMM — a real biological difference in what that
  combination of TF states produces, not a statistical artifact.

**Direct test**: hard-binarize cells (>0.5) into their exact combinatorial leaf — the same
indexing `get_rules`/`parent_heatmap` use — for 5 genes (`LMX1B`, `ASCL1`, `NFIB`,
`TCF7L1`, `EHF`; 4 to 8 regulators each), and compare each leaf's mean *actual* target
value against GEMM's own fitted rule value for that *exact* leaf. This removes the
composition confound entirely — cells are grouped by their precise combinatorial state,
not by a marginal summary.

**Positive control**: GEMM's own held-out test data shows excellent leaf-conditional
agreement (mean |residual| 0.03-0.06 across all 5 genes) — confirms the test itself works
and the rules are genuinely well-fit in-distribution.

**organoid_shGFP does not replicate this, even leaf-conditionally**: mean |residual| ≈
0.40 across the same 5 genes (only 19% of well-populated leaves agree within 0.15).
Concretely, `EHF`'s leaf where both regulators are OFF: GEMM's rule value is 0.17;
organoid_shGFP's actual mean for cells in that *exact* combinatorial state is 0.68 — nearly
inverted, with the population-mixture confound already removed. Same pattern for `ASCL1`,
`NFIB`, `TCF7L1`, `LMX1B` — large, systematic residuals persist leaf-by-leaf.

**Conclusion: this is genuine rewiring, not a composition artifact.** The same
combination of TF states really does produce a different target-gene outcome in organoid
vs. GEMM. This is consistent with real, well-documented biology: TF *activity* (as opposed
to TF presence/expression level, which is what BoBa-T's regulator values measure) depends
on cofactor availability and chromatin/signaling context — factors that plausibly differ
between an intact in vivo tumor (immune/stromal signaling, native ECM, systemic hormones)
and an organoid grown in defined culture media, even for identical genetic background.
BoBa-T's rules encode "what does this TF combination mean" *in the context it was fit in*
— they are not guaranteed, and here are directly shown not, to encode a context-independent
causal law.

Outputs: `leaf_conditional_agreement_organoid_shgfp.csv`.

## 10. Full-scale robust-core map: 53 genes x 33 external samples (`diagnose_leaf_conditional_agreement_full.py`)

§9 tested 5 genes against 1 sample. Generalized to every one of 6667's network genes
against every one of the 33 scored external samples (allografts, human tumors, organoid
variants, mets_compiled), building the per-gene x per-sample transferability matrix
proposed in `BoBa-T_hyperparameters.md` §11 (transferability = cell-count-weighted
fraction of a gene's well-populated combinatorial leaves, across a sample, whose actual
mean agrees with GEMM's fitted rule value within 0.15).

**Validates the whole approach**: GEMM's own held-out test data scores 0.986 mean
transferability (near-perfect positive control). And **per-sample mean transferability
correlates with that sample's mean R² at r=0.96** across all 33 samples — by far the
strongest predictor found in this entire investigation (vs. §4's sign-flip rate at -0.72,
§7's identity-PC-variance-fraction at 0.65). This makes sense: it's the most direct
possible measurement of "does the fitted rule actually hold in this sample," with the
population-composition confound removed by construction.

**Caveat, important for interpretation**: the network's 11 self-loop-only "source" nodes
(`CREB1`, `TCF4`, `ESR1`, `SOX9`, `RBPJ`, `NR6A1`, `NFYC`, `JUND`, `ZEB1`, `STAT1`,
`TFDP1` — same list the existing pipeline already excludes from averaged-ROC plots for
the same reason, "artificially high ROC") score a trivial 1.0 transferability across
every sample. A self-loop rule predicts a gene largely from its own current level, which
trivially "transfers" anywhere — this is not a real generalization result and must be
excluded before ranking genes.

**Real result, excluding self-loop nodes** (42 genes with >=2 regulators):
- **Most transferable**: `TEAD1` (0.63), `PROX1` (0.52), `BACH1` (0.50), `HSF2` (0.50),
  `EGR1` (0.48), `ZBTB20` (0.47), `NFATC2` (0.47), `STAT2` (0.46), `NCAM1` (0.46), `SIX4`
  (0.45). Even the single best real (non-self-loop) gene only reaches 0.63 — no edge in
  this network transfers reliably across the *full*, highly heterogeneous population of
  33 external samples (mouse allografts, human tumors, mouse organoid culture all mixed
  together). This is a realistic ceiling, not a bug: the population deliberately spans
  very different biological contexts.
- **Least transferable**: `NFIX` (0.23), `JUNB` (0.23), `KMT2A` (0.23), `TCF7L2` (0.25),
  `FOS` (0.30), `LMX1B` (0.31), `MEIS2` (0.31), `EHF` (0.33), `JUN` (0.33), `ASCL1` (0.33)
  — notably, these are largely the same genes flagged throughout this investigation as
  worst-validating/most-rewired (`ASCL1`, `LMX1B`, `EHF`, `JUN` all appeared in §4's and
  §9's per-gene analyses), a good internal consistency check on the method.

**Practical use**: this matrix (`leaf_conditional_transferability_matrix.csv`) can be
re-aggregated over any subset of samples relevant to a specific claim — e.g. restricting
to just the allograft+human-tumor (in-vivo) samples to get a "robust core for in-vivo
extrapolation" ranking, separate from the full population's necessarily more conservative
one. Any attractor-prediction or perturbation claim in the paper involving a
bottom-ranked gene for the relevant target context should be scoped accordingly rather
than presented as a general claim.

Outputs: `leaf_conditional_transferability_matrix.csv` (full per-gene x per-sample
matrix), `leaf_conditional_robust_core_per_gene.csv` (per-gene summary),
`leaf_conditional_per_sample_summary.csv` (per-sample summary + r=0.96 check).

## 11. Validation-robustness checks: is the whole R²/F1/AUC comparison a metric artifact? (`plot_organoid_shgfp_by_line.py`, scrambled-null pipeline, `all_samples_metrics_by_category*.csv`)

Separate from the domain-shift mechanism (§1-10), a more basic question underlies every
result in this file: are the external-validation scores themselves trustworthy, or could
the whole R²/F1/AUC comparison across categories be a metric artifact rather than a real
signal?

**(a) organoid_shGFP's poor validation isn't one bad replicate dragging down the average.**
Split shGFP by its 5 constituent sequencing lines (`D_Sample1/4/6` etc.) and scored each
independently (`organoid_shgfp_by_line_metrics.csv`). R²/F1/AUC are consistent across all
5 lines — no single line is an outlier inflating or deflating the pooled shGFP score.

**(b) Real validation scores are not a metric artifact: every category clears a
scrambled-data null.** Built a scrambled-label null (`scrambled_null_*.csv`: regulator
values randomly permuted within each sample, 50+ iterations per sample/gene) and compared
every external sample category's real R²/F1/AUC against its own null distribution
(`all_samples_r2_by_category.png`, `all_samples_f1_by_category.png`,
`all_samples_auc_by_category.png`). Every category — allograft, human tumor, organoid,
mets_compiled — scores clearly above its scrambled null. The domain-shift effects in §1-10
are differences in the *size* of a real signal, not the presence/absence of one.

**(c) ~20% of the network's genes get a "free" boost from self-referencing regulation,
inflating both the null and the real headline numbers alike.** 11 of the network's 53
genes are their own regulator (the same self-loop-only "source" nodes flagged in §10:
`CREB1`, `TCF4`, `ESR1`, `SOX9`, `RBPJ`, `NR6A1`, `NFYC`, `JUND`, `ZEB1`, `STAT1`, `TFDP1`).
A rule that predicts a gene mostly from its own current value scores near-perfectly
(AUC≈1.0, r≈1.0) even on scrambled data, since permuting *other* regulators barely touches
a self-loop-dominated prediction. That means these 11 genes inflate the null baseline and
the real score by the same mechanism, in every category, everywhere they appear.

**(d) Once self-loop genes are excluded, most categories collapse to near-zero — only
allograft and TKO retain a clearly real, cross-gene signal.** Re-scored every category
excluding the 11 self-loop genes (`all_samples_metrics_by_category_excl_selfloops.csv`,
`all_samples_r2_by_category_excl_selfloops.png` and the F1/AUC equivalents):
human_tumor, organoid, and mets_compiled validation collapses to near-zero or negative R²
once the free self-loop boost is removed. **Allograft and TKO are the exception** — they
keep a real, non-trivial cross-gene predictive signal even without self-loop genes
propping up the score. This sharpens every category-level comparison in this file: the
categories that looked "okay" partly because of self-loop genes turn out to have little
real cross-gene signal once those are excluded, while allograft/TKO's validation is
genuinely robust.

**(e) `TKO-luc` was miscategorized as an allograft — it's not one, and it's the
best-scoring sample overall.** `TKO-luc` lives in the allograft data folder but isn't
actually an allograft; recategorized into its own category for the plots above. Once
separated out, it's the single best-scoring sample in the entire 33-sample set (consistent
with its high `relative diversity` and low sign-flip rate already noted in §4/§6).

Outputs: `organoid_shgfp_by_line.png`/`.pdf` + `organoid_shgfp_by_line_metrics.csv`;
`all_samples_r2_by_category.png`, `all_samples_f1_by_category.png`,
`all_samples_auc_by_category.png` (+ `_excl_selfloops` variants and matching `.pdf`s);
`all_samples_metrics_by_category_incl_selfloops.csv`,
`all_samples_metrics_by_category_excl_selfloops.csv`; `scrambled_null_*.csv` (per-sample
and per-gene scrambled-null distributions).

## 12. Does validation quality depend on a sample's own ASCL1 expression? (`ascl1_dependence_analysis.py`, `plot_ascl1_dependence.py`)

Direct test of a specific hypothesis: since the 6667 network was fit on ASCL1-driven GEMM
tumors, do allografts and human tumors (MSK) that are themselves more ASCL1-high validate
better? Checked for both R² and AUC, since boba-T's fitted object is inherently binary and
CellOracle-style continuous fit is not (same reasoning as the benchmarking deck's R²/AUC
pairing) — here both metrics are boba-T's own, so this is about which one is more sensitive
to the effect, not a cross-method comparison.

**NEUROD1, YAP1, POU2F3 could not be checked — real data gap, not a shortcut.** None of the
three are in the 6667 network's 53 genes; none are in the 86-gene curated TF panel these
external-validation samples were subset to (`data/{allografts,human_tumor_MSK}/adata_<sample>.csv`,
confirmed by listing all 86 column names directly); and the raw full-transcriptome source
files that panel was subset from (`data/external_validation_looms/.../{sample}_*.h5ad`, per
`preprocess_adata.py`'s own commented-out pipeline) no longer exist on disk — only the
already-subset CSVs and raw per-lane `.loom` files (velocyto output, no sample-ID mapping)
remain. ASCL1 itself is a real network gene, unaffected by this gap.

**Method**: for each of 12 allograft and 16 human-tumor (MSK) samples with a scored
`summary_stats.csv`, took the mean of ASCL1's own `actual` column in
`accuracy_plots/ASCL1_validation.csv` — boba-T's GMM-normalized (0–1) target value, on that
sample's own held-out validation cells, i.e. the *exact* cells and processing already used to
compute that sample's R²/AUC, so this introduces no new data loading or normalization choice.
Correlated this per-sample mean against per-sample mean R² and mean AUC (all 53 genes; excluding
the 11 self-loop genes gives virtually identical correlations, since exclusion shifts every
sample's score by a similar amount).

**Result: a real but modest positive trend, significant for AUC, not quite for R², and not
supported within allografts alone.**

| population | metric | Pearson r | p | n |
|---|---|---|---|---|
| all (allograft + human_tumor) | R² | 0.34 | 0.074 | 28 |
| all (allograft + human_tumor) | AUC | **0.40** | **0.033** | 28 |
| allograft only | R² | 0.13 | 0.70 | 12 |
| allograft only | AUC | 0.15 | 0.64 | 12 |
| human_tumor only | R² | 0.40 | 0.12 | 16 |
| human_tumor only | AUC | 0.47 | 0.068 | 16 |

**Two things temper this being a clean confirmation:**
1. **ASCL1's own range across every sample here is narrow (0.43–0.57)** — there's no clearly
   ASCL1-negative (e.g. NEUROD1/YAP1/POU2F3-subtype) tumor in this specific cohort to contrast
   against, in either allografts (all derived from the same ASCL1-driven GEMM to begin with) or
   this particular MSK human-tumor set. The correlation is real over the range that exists, but
   that range may not span the full biological contrast the hypothesis is really about.
2. **The best-validating allograft is a counter-example, not a confirming case.** `TKO-luc` has
   the best R²/AUC of any allograft (0.55/0.88) but a middling ASCL1 value (0.504, below the
   allograft median) — while `1L`, the single highest-ASCL1 allograft (0.573), only validates
   second-best (R² 0.36). This is why the allograft-only correlation is essentially flat
   (r=0.13–0.15) despite the pooled and human-tumor-only trends being positive: allografts don't
   show much of this relationship on their own, human tumors do more of the work in the pooled
   result. In human tumors specifically, the pattern is cleaner: all 4 best-validating samples
   (`RU1311`, `RU1124`, `RU1065`, `RU1195`) sit above the within-group ASCL1 median, and the
   worst (`RU1322`, `RU1215`) sit at or near the bottom.

**Bottom line**: the hypothesis gets real, if not overwhelming, support — mainly from the human
tumor cohort and mainly on AUC — but isn't a clean story in allografts, and the ASCL1 range
available to test it is narrower than the underlying A/N/Y/P subtype question really calls for.
Confirming it properly would need NEUROD1/YAP1/POU2F3 (or at least a wider ASCL1 range) from a
source outside this repo's currently tracked data.

**Follow-up: high-vs-low ASCL1 t-test (median split) — direction matches, but no split reaches
significance, and it's less sensitive than the correlation itself.** Split all 28 samples (and
each category separately) into high/low ASCL1 groups at the pooled median (0.519) and ran
Welch's t-test on mean R² and mean AUC:

| population | metric | n high / low | high mean | low mean | Welch t | p |
|---|---|---|---|---|---|---|
| all | R² | 14 / 14 | 0.266 | 0.207 | 1.09 | 0.284 |
| all | AUC | 14 / 14 | 0.770 | 0.740 | 1.38 | 0.178 |
| allograft | R² | 6 / 6 | 0.265 | 0.239 | 0.37 | 0.726 |
| allograft | AUC | 6 / 6 | 0.772 | 0.760 | 0.44 | 0.677 |
| human_tumor | R² | 8 / 8 | 0.264 | 0.184 | 1.00 | 0.336 |
| human_tumor | AUC | 8 / 8 | 0.765 | 0.729 | 1.11 | 0.287 |

The high-ASCL1 group scores higher on every row (medium effect sizes, Cohen's d 0.21–0.56) —
directionally consistent with the correlation above — but none clear p<0.05 at these group
sizes. Notably weaker than the pooled AUC correlation (r=0.40, p=0.033): a median split throws
away the continuous ASCL1 signal, and `TKO-luc` (allograft's best R²/AUC) falls in the *low*
group here, pulling that group's mean up and directly working against the split. Median-split
group comparisons and the continuous correlation are answering slightly different questions
(mean difference vs. linear trend) — treat the correlation as the more sensitive, and this
t-test as a check on whether it depends on that specific framing (it doesn't reverse, but it
does weaken).

Outputs: [ascl1_dependence_by_sample.csv](ascl1_dependence_by_sample.csv) (per-sample table),
[ascl1_dependence_correlations.csv](ascl1_dependence_correlations.csv) (all 12 population×metric
combinations), [ascl1_dependence_scatter.png](ascl1_dependence_scatter.png),
[ascl1_high_vs_low_ttest.csv](ascl1_high_vs_low_ttest.csv), [ascl1_high_vs_low_boxplot.png](ascl1_high_vs_low_boxplot.png).

**Follow-up: NEUROD1/YAP1/POU2F3 recovered for allografts (real raw data, not a workaround) —
result: every allograft is unambiguously ASCL1-subtype, so the A-vs-other-subtype test the
premise calls for cannot be run there at all.** The raw `.loom` files this repo already has
(`data/external_validation_looms/allografts/*.loom`, velocyto output) turned out to carry the
full 32,285-gene mouse transcriptome, including all four subtype markers — the earlier "not
available" conclusion was about the *curated, already-subsetted* CSVs, not these. There was no
loom-filename-to-sample mapping anywhere in the repo, so it was reconstructed directly: each
loom's own `CellID` barcode-prefix (e.g. `possorted_genome_bam_OMHB6`) was checked against the
unique prefix in each allograft's own `accuracy_plots/*_validation.csv` — an exact string match
for one and only one sample, for every one of the 12 allografts (not a guess; verified
programmatically, and the held-out-cell match rate confirms it: 100% of every sample's held-out
validation cells were found by exact `CellID` string in that sample's mapped loom file).
Extracted Ascl1/Neurod1/Yap1/Pou2f3 raw spliced counts for exactly those held-out cells, library-
size-normalized (log1p, counts per 10k) per cell, then averaged per sample.

**Result: Neurod1, Yap1, and Pou2f3 are essentially undetectable in every single allograft**
(mean log1p-CP10k 0.0000–0.0291, vs. Ascl1's 1.01–2.33) — the highest any of the three ever
reaches is Yap1 at 0.029 in `TKO-luc`, two orders of magnitude below Ascl1's floor. All 12/12
allografts classify as "A" by dominant-marker. This isn't surprising in retrospect — these are
all derived from the same ASCL1-driven GEMM, an engineered isogenic model, not a naturally
heterogeneous patient population — but it does mean **the subtype-group comparison the original
hypothesis calls for genuinely cannot be run on allografts**: there is no N, Y, or P group to
compare "A" against.

**A further, less expected result: within allografts, *raw* ASCL1 level shows no positive
relationship with validation quality — if anything, a non-significant negative one** (R²
r=-0.32, p=0.30; AUC r=-0.27, p=0.39; n=12) — the opposite sign from the pooled,
boba-T-normalized-ASCL1 trend in the section above. `TKO-luc`, the best-validating allograft,
sits at a middling raw Ascl1 level; the two highest-raw-Ascl1 allografts (`3L`, `mt2`) are among
the worst-validating. Raw Ascl1 and boba-T's own GMM-normalized ASCL1 `actual` value barely
correlate with each other within allografts either (r=-0.19, p=0.55) — the two are measuring
different things (GMM normalization targets a bimodal ON/OFF call, not a linear expression
level), so a result derived from one doesn't necessarily transfer to the other. Net effect:
**the allograft side of the original hypothesis gets no support from real marker data, however
it's measured** — the earlier positive pooled trend was being carried by the human-tumor cohort.

**Human tumors (MSK) could not get the same treatment — the gap here is real, not skipped.**
No raw `.loom`, `.h5ad`, or any full-transcriptome file for any of the 16 MSK samples exists
anywhere in this repo (confirmed by a targeted search); only the pre-subsetted, ASCL1-only
86-gene CSVs remain, and the original source files `preprocess_adata.py` references
(`data/external_validation_looms/human_tumors/{sample}_human_tumor_data.h5ad`) are not on disk.
Unlike the allografts, the human tumors are real patient-derived samples where non-ASCL1
subtypes (N/Y/P) are biologically plausible — this is exactly the population where the
recovered-loom trick would matter most, and exactly the one it couldn't be applied to. If you
have access to the original MSK SCLC atlas data (Chan et al.-style h5ad/Seurat objects, or the
raw FASTQ/BAM this repo's own `human_tumors_MSK_clusters.csv` was derived from), point me to it
and this can be redone properly with all four markers on the population that actually needs it.

Outputs: [allograft_anyp_marker_expression.csv](allograft_anyp_marker_expression.csv) (per-sample,
all 4 markers), [allograft_anyp_marker_expression.png](allograft_anyp_marker_expression.png),
[allograft_raw_ascl1_vs_metrics.png](allograft_raw_ascl1_vs_metrics.png).

**Resolved: the human-tumor gap above, closed with the paper's own published subtype calls —
result: real, significant support for the original hypothesis.** Chan et al. 2021 (Cancer Cell,
the actual source publication for this repo's `human_tumor_MSK` cohort — patient IDs match
exactly) already determined each patient's major-subclone SCLC subtype and published it in
Figure S2A (bar plot of per-sample subtype population fraction, provided directly by the user
as a supplementary PDF). Read at 400 DPI to resolve the per-sample bars precisely rather than
trusting low-res text extraction. All 16 of this repo's human-tumor samples matched unambiguously
(2 patients, RU1181 and RU1144, were biopsied at two sites each in the original paper — LN and
tumor — but both sites agreed on the same subtype call in both cases, so the merged sample this
repo uses is unambiguous either way):

| subtype | samples |
|---|---|
| SCLC-A (n=11) | RU1065, RU1066, RU1080, RU1108, RU1124, RU1144, RU1145, RU1152, RU1195, RU1229, RU1311 |
| SCLC-N (n=4) | RU1181, RU1215, RU1231, RU1293 |
| SCLC-P (n=1) | RU1322 |

(No SCLC-Y identified in the original cohort at all — consistent with this repo's 6667 network,
which also has no meaningful YAP1 signal.)

**SCLC-A validates significantly better than non-A (N+P) on both metrics** — the real,
direct test of the original hypothesis, using ground-truth published labels instead of a
continuous expression proxy:

| metric | SCLC-A mean (n=11) | non-A mean (n=5) | Welch t | p |
|---|---|---|---|---|
| R² | 0.267 | 0.130 | 2.24 | **0.042** |
| AUC | 0.766 | 0.705 | 2.46 | **0.027** |

Restricting to SCLC-A vs. SCLC-N alone (dropping the single SCLC-P sample, `RU1322`, which is
itself one of the worst-validating samples and was helping drive the above result) weakens both
to trend-level (R² p=0.074, AUC p=0.054, n=11 vs. 4) — expected at this sample size, and
consistent with the direction holding, not reversing.

**This is the cleanest result in the whole ASCL1-dependence investigation**: unlike the
continuous-ASCL1-expression correlation (real but modest, driven unevenly across categories) and
the allograft raw-marker check (null/negative, but on a population with no real subtype
variation to begin with), this uses the *actual* biological ground truth the hypothesis was
about — and confirms it. The network was fit on ASCL1-driven mouse tumors, and it validates
measurably better on the human tumors that are themselves ASCL1-subtype.

Outputs: [human_tumor_subtype_from_chan_et_al.csv](human_tumor_subtype_from_chan_et_al.csv)
(per-sample subtype + R²/AUC/ASCL1), [human_tumor_subtype_group_test.png](human_tumor_subtype_group_test.png).

**Follow-up: relapsed vs. not (allograft name contains "R") — no signal, but essentially
untestable at n=2.** Of the 12 allografts, only `2LR` and `mt4Rf` are labeled relapsed; the
other 10 (`1L`, `2L`, `3L`, `5B`, `TKO-luc`, `mt2`–`mt6`) are not. Welch's t-test: R² 0.265 vs.
0.250 (t=0.30, p=0.771); AUC 0.769 vs. 0.765 (t=0.18, p=0.864) — means nearly identical, no
real difference detectable, but a 2-vs-10 comparison has essentially no power to detect one
even if it existed. Take this as descriptive only.

Output: [allograft_relapsed_vs_not.png](allograft_relapsed_vs_not.png).
